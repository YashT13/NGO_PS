create table LoginInfo (Username varchar(30) primary key, [Password] varchar(30))
select * from Basketball
alter table Student drop column Attendance
select * from Chess


create Proc SP_InsertReg
{ 
	StudentName varchar(20)
}
As 
Begin
	insert into